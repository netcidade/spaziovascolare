# Requirements

## 1. Table Stakes
- Appwrite integration must securely fetch data without exposing private variables to the frontend.
- All existing CRUD operations (Create, Read, Update, Delete) in the Admin dashboard must work correctly against the actual Appwrite backend collections.

## 2. Features
- [ ] Connect local repository to remote origin: `https://github.com/netcidade/spaziovascolare.git` and perform initial push.
- [ ] Systematically review and verify Appwrite connection (database schemas, error boundaries, data fetching logic).
