import {
  BaseApp,
  createAssetLink,
  createConsoleLogger,
  createModuleScriptElement,
  createStylesheetElementSet
} from "./chunk-KD7ABTRK.js";
import {
  Pipeline,
  RedirectSinglePageBuiltModule,
  findRouteToRewrite
} from "./chunk-PAKFPEJI.js";
import "./chunk-D4U6XPBB.js";
import "./chunk-J4QMGZNF.js";
import "./chunk-HAFW5F4N.js";
import "./chunk-BDYKFCAO.js";
import "./chunk-C6CIJVMS.js";
import {
  deserializeManifest,
  deserializeRouteData,
  deserializeRouteInfo,
  serializeRouteData,
  serializeRouteInfo
} from "./chunk-KDHD2USM.js";
import "./chunk-OXLQCLZM.js";
import "./chunk-VUUZECRF.js";
import "./chunk-GVFHKA3H.js";
import "./chunk-GSVIDLLM.js";
import "./chunk-QF6GMXFP.js";
import "./chunk-4DU34QGE.js";
import "./chunk-PYWK5OID.js";
import "./chunk-Y3KCRQST.js";
import "./chunk-ZEWIURSN.js";
import "./chunk-STGKB6CW.js";
import "./chunk-JMXJCI3S.js";
import "./chunk-HTFZH2OR.js";
import "./chunk-CVXNHYZZ.js";
import "./chunk-K5YRUHQY.js";
import "./chunk-3C2TUJPP.js";
import "./chunk-SI3LEWOE.js";
import "./chunk-ACTHJLYT.js";
import "./chunk-CR46HLCO.js";
import "./chunk-UI54HI3C.js";
import "./chunk-L6YMUYAO.js";
import "./chunk-S3NXP5Y3.js";
import "./chunk-W5OYJRDO.js";
import "./chunk-K7R6D5YG.js";
import "./chunk-BA7OZ3KE.js";
import "./chunk-EV2HKNXH.js";
import "./chunk-BSZJJDHH.js";
import "./chunk-WSAXZJT2.js";
import "./chunk-Q2U4AE3K.js";
import "./chunk-5ZHQYUIL.js";
import "./chunk-JL2WL2Y6.js";
import "./chunk-WDRNXV4L.js";
import "./chunk-FQE2ZCDX.js";
import "./chunk-D4EPHHYI.js";
import "./chunk-H672BWDB.js";
import "./chunk-PUYLG7XE.js";
import {
  getFallbackRoute,
  routeIsFallback,
  routeIsRedirect
} from "./chunk-QOFNKQNG.js";
import "./chunk-IEIBBQNB.js";
import "./chunk-LOGPQGUP.js";
import "./chunk-Z45NPJWO.js";
import "./chunk-JMGHSZHP.js";
import "./chunk-JYSSCBAJ.js";
import "./chunk-4ECSYGN6.js";
import {
  init_define_globalThis_process_env,
  init_define_global_process_env,
  init_define_process_env
} from "./chunk-3EZRQDAS.js";

// node_modules/astro/dist/core/app/entrypoints/index.js
init_define_global_process_env();
init_define_globalThis_process_env();
init_define_process_env();

// node_modules/astro/dist/core/app/app.js
init_define_global_process_env();
init_define_globalThis_process_env();
init_define_process_env();

// node_modules/astro/dist/core/app/pipeline.js
init_define_global_process_env();
init_define_globalThis_process_env();
init_define_process_env();
var AppPipeline = class _AppPipeline extends Pipeline {
  getName() {
    return "AppPipeline";
  }
  static create({ manifest, streaming }) {
    const resolve = async function resolve2(specifier) {
      if (!(specifier in manifest.entryModules)) {
        throw new Error(`Unable to resolve [${specifier}]`);
      }
      const bundlePath = manifest.entryModules[specifier];
      if (bundlePath.startsWith("data:") || bundlePath.length === 0) {
        return bundlePath;
      } else {
        return createAssetLink(bundlePath, manifest.base, manifest.assetsPrefix);
      }
    };
    const logger = createConsoleLogger(manifest.logLevel);
    const pipeline = new _AppPipeline(
      logger,
      manifest,
      "production",
      manifest.renderers,
      resolve,
      streaming,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0
    );
    return pipeline;
  }
  async headElements(routeData) {
    const { assetsPrefix, base } = this.manifest;
    const routeInfo = this.manifest.routes.find(
      (route) => route.routeData.route === routeData.route
    );
    const links = /* @__PURE__ */ new Set();
    const scripts = /* @__PURE__ */ new Set();
    const styles = createStylesheetElementSet(routeInfo?.styles ?? [], base, assetsPrefix);
    for (const script of routeInfo?.scripts ?? []) {
      if ("stage" in script) {
        if (script.stage === "head-inline") {
          scripts.add({
            props: {},
            children: script.children
          });
        }
      } else {
        scripts.add(createModuleScriptElement(script, base, assetsPrefix));
      }
    }
    return { links, styles, scripts };
  }
  componentMetadata() {
  }
  async getComponentByRoute(routeData) {
    const module = await this.getModuleForRoute(routeData);
    return module.page();
  }
  async getModuleForRoute(route) {
    for (const defaultRoute of this.defaultRoutes) {
      if (route.component === defaultRoute.component) {
        return {
          page: () => Promise.resolve(defaultRoute.instance)
        };
      }
    }
    let routeToProcess = route;
    if (routeIsRedirect(route)) {
      if (route.redirectRoute) {
        routeToProcess = route.redirectRoute;
      } else {
        return RedirectSinglePageBuiltModule;
      }
    } else if (routeIsFallback(route)) {
      routeToProcess = getFallbackRoute(route, this.manifest.routes);
    }
    if (this.manifest.pageMap) {
      const importComponentInstance = this.manifest.pageMap.get(routeToProcess.component);
      if (!importComponentInstance) {
        throw new Error(
          `Unexpectedly unable to find a component instance for route ${route.route}`
        );
      }
      return await importComponentInstance();
    } else if (this.manifest.pageModule) {
      return this.manifest.pageModule;
    }
    throw new Error(
      "Astro couldn't find the correct page to render, probably because it wasn't correctly mapped for SSR usage. This is an internal error, please file an issue."
    );
  }
  async tryRewrite(payload, request) {
    const { newUrl, pathname, routeData } = findRouteToRewrite({
      payload,
      request,
      routes: this.manifest?.routes.map((r) => r.routeData),
      trailingSlash: this.manifest.trailingSlash,
      buildFormat: this.manifest.buildFormat,
      base: this.manifest.base,
      outDir: this.manifest?.serverLike ? this.manifest.buildClientDir : this.manifest.outDir
    });
    const componentInstance = await this.getComponentByRoute(routeData);
    return { newUrl, pathname, componentInstance, routeData };
  }
};

// node_modules/astro/dist/core/app/app.js
var App = class extends BaseApp {
  createPipeline(streaming) {
    return AppPipeline.create({
      manifest: this.manifest,
      streaming
    });
  }
  isDev() {
    return false;
  }
  // Should we log something for our users?
  logRequest(_options) {
  }
};

// node_modules/astro/dist/core/app/common.js
init_define_global_process_env();
init_define_globalThis_process_env();
init_define_process_env();
function toRoutingStrategy(routing, domains) {
  let strategy;
  const hasDomains = domains ? Object.keys(domains).length > 0 : false;
  if (routing === "manual") {
    strategy = "manual";
  } else {
    if (!hasDomains) {
      if (routing?.prefixDefaultLocale === true) {
        if (routing.redirectToDefaultLocale) {
          strategy = "pathname-prefix-always";
        } else {
          strategy = "pathname-prefix-always-no-redirect";
        }
      } else {
        strategy = "pathname-prefix-other-locales";
      }
    } else {
      if (routing?.prefixDefaultLocale === true) {
        if (routing.redirectToDefaultLocale) {
          strategy = "domains-prefix-always";
        } else {
          strategy = "domains-prefix-always-no-redirect";
        }
      } else {
        strategy = "domains-prefix-other-locales";
      }
    }
  }
  return strategy;
}
var PREFIX_DEFAULT_LOCALE = /* @__PURE__ */ new Set([
  "pathname-prefix-always",
  "domains-prefix-always",
  "pathname-prefix-always-no-redirect",
  "domains-prefix-always-no-redirect"
]);
var REDIRECT_TO_DEFAULT_LOCALE = /* @__PURE__ */ new Set([
  "pathname-prefix-always-no-redirect",
  "domains-prefix-always-no-redirect"
]);
function fromRoutingStrategy(strategy, fallbackType) {
  let routing;
  if (strategy === "manual") {
    routing = "manual";
  } else {
    routing = {
      prefixDefaultLocale: PREFIX_DEFAULT_LOCALE.has(strategy),
      redirectToDefaultLocale: !REDIRECT_TO_DEFAULT_LOCALE.has(strategy),
      fallbackType
    };
  }
  return routing;
}
export {
  App,
  AppPipeline,
  BaseApp,
  createConsoleLogger,
  deserializeManifest,
  deserializeRouteData,
  deserializeRouteInfo,
  fromRoutingStrategy,
  serializeRouteData,
  serializeRouteInfo,
  toRoutingStrategy
};
//# sourceMappingURL=astro_app.js.map
